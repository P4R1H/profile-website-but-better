import { TesseractCellData } from '@/types/types';
import React from 'react';

export const winsData: TesseractCellData = {
  id: "wins",
  title: "WINS",
  subtitle: "Good enough just isn't good enough."
};
